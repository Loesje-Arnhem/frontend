query GET_POSTERS($first: Int, $after: String, $search: String, $notIn: [ID]) {
  posters(
    first: $first
    after: $after
    where: { search: $search, notIn: $notIn }
  ) {
    pageInfo {
      hasNextPage
      endCursor
      hasPreviousPage
      startCursor
    }
    edges {
      node {
        id
        posterId
        title
        slug
        featuredImage {
          id
          medium: sourceUrl(size: MEDIUM)
        }
      }
    }
  }
}
