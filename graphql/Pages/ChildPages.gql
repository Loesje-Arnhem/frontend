query ChildPages($parent: String, $notIn: [ID]) {
  pages: pages(where: { parent: $parent, notIn: $notIn }) {
    edges {
      node {
        id
        databaseId
        title
        uri
        excerpt
      }
    }
  }
}
